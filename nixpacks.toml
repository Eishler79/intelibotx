[phases.setup]
nixPkgs = ["python39"]

[phases.install]
cmds = ["cd intelibotx-api && pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "cd intelibotx-api && python -m uvicorn main:app --host=0.0.0.0 --port=$PORT"